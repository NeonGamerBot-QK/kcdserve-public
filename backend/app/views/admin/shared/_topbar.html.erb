<nav class="bg-white dark:bg-slate-900 border-b border-slate-200 dark:border-slate-700 px-6 py-3">
  <div class="flex justify-between items-center">
    <div class="flex items-center space-x-3">
      <div class="w-9 h-9 rounded-full bg-accent-500 flex items-center justify-center">
        <span class="font-semibold text-white text-sm"><%= current_user.first_name[0] %></span>
      </div>
      <span class="text-slate-700 dark:text-slate-300 text-sm">
        Signed in as <strong class="font-semibold"><%= current_user.full_name %></strong>
        <span class="ml-1 px-2 py-0.5 rounded-full bg-slate-100 dark:bg-slate-700 text-slate-500 dark:text-slate-400 text-xs"><%= current_user.role.humanize %></span>
      </span>
    </div>
    <div>
      <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "px-4 py-1.5 border border-slate-300 dark:border-slate-600 rounded-lg text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800 text-sm font-medium" %>
    </div>
  </div>
</nav>
