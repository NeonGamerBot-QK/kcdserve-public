<nav class="bg-white border-b border-gray-200 px-4 py-3">
  <div class="flex justify-between items-center">
    <span class="text-gray-700">
      Signed in as <strong><%= current_user.full_name %></strong> (<%= current_user.role.humanize %>)
    </span>
    <div>
      <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "px-3 py-1 border border-gray-300 rounded text-gray-700 hover:bg-gray-100 text-sm" %>
    </div>
  </div>
</nav>
