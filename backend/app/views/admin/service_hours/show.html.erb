<h1 class="text-2xl font-semibold text-slate-900 dark:text-white mb-6">Review Service Hour</h1>

<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
  <div class="md:col-span-2">
    <div class="bg-white dark:bg-slate-800 rounded-2xl border border-gray-200 dark:border-slate-700 p-6 mb-6">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="space-y-2">
          <% if @service_hour.title.present? %>
            <p class="text-sm"><span class="font-medium text-slate-500 dark:text-slate-400">Title:</span> <span class="text-slate-900 dark:text-white"><%= @service_hour.title %></span></p>
          <% end %>
          <p class="text-sm">
            <span class="font-medium text-slate-500 dark:text-slate-400">Volunteer:</span>
            <% if @service_hour.user %>
              <%= link_to @service_hour.user.full_name, admin_user_path(@service_hour.user), class: "text-primary-500 hover:text-primary-700" %>
              <% if @service_hour.user.deleted? %>
                <span class="ml-1 px-2 py-0.5 rounded-full bg-slate-100 dark:bg-slate-700 text-slate-500 dark:text-slate-400 text-xs">Deactivated</span>
              <% end %>
            <% else %>
              <span class="text-slate-400">Deleted User</span>
            <% end %>
          </p>
          <p class="text-sm"><span class="font-medium text-slate-500 dark:text-slate-400">Date:</span> <span class="text-slate-900 dark:text-white"><%= @service_hour.service_date.strftime("%B %d, %Y") %></span></p>
          <p class="text-sm"><span class="font-medium text-slate-500 dark:text-slate-400">Hours:</span> <span class="text-slate-900 dark:text-white font-semibold"><%= @service_hour.hours %></span></p>
          <p class="text-sm"><span class="font-medium text-slate-500 dark:text-slate-400">Category:</span> <span class="text-slate-900 dark:text-white"><%= @service_hour.category.name %></span></p>
          <% if @service_hour.location.present? %>
            <p class="text-sm"><span class="font-medium text-slate-500 dark:text-slate-400">Location:</span> <span class="text-slate-900 dark:text-white"><%= @service_hour.location %></span></p>
          <% end %>
        </div>
        <div class="space-y-2">
          <p class="text-sm"><span class="font-medium text-slate-500 dark:text-slate-400">Group:</span> <span class="text-slate-900 dark:text-white"><%= @service_hour.group&.name || "N/A" %></span></p>
          <p class="text-sm">
            <span class="font-medium text-slate-500 dark:text-slate-400">Status:</span>
            <span class="ml-1 px-2 py-0.5 rounded-full text-xs font-medium <%= @service_hour.approved? ? 'bg-green-50 dark:bg-green-950 text-status-approved' : @service_hour.pending? ? 'bg-amber-50 dark:bg-amber-950 text-status-pending' : 'bg-red-50 dark:bg-red-950 text-status-rejected' %>">
              <%= @service_hour.status.humanize %>
            </span>
          </p>
          <% if @service_hour.supervisor_status.present? %>
            <p class="text-sm">
              <span class="font-medium text-slate-500 dark:text-slate-400">Supervisor Verified:</span>
              <span class="ml-1 px-2 py-0.5 rounded-full text-xs font-medium <%= @service_hour.supervisor_approved? ? 'bg-green-50 dark:bg-green-950 text-status-approved' : 'bg-red-50 dark:bg-red-950 text-status-rejected' %>">
                <%= @service_hour.supervisor_status.humanize %>
              </span>
              <% if @service_hour.contact_name.present? %>
                <span class="text-slate-400 dark:text-slate-500 text-xs">(by <%= @service_hour.contact_name %>)</span>
              <% end %>
            </p>
          <% elsif @service_hour.contact_email.present? %>
            <p class="text-sm">
              <span class="font-medium text-slate-500 dark:text-slate-400">Supervisor Verified:</span>
              <span class="ml-1 px-2 py-0.5 rounded-full text-xs font-medium bg-amber-50 dark:bg-amber-950 text-status-pending">Awaiting Response</span>
            </p>
          <% end %>
        </div>
      </div>
      <hr class="my-4 border-gray-200 dark:border-slate-700">
      <h6 class="font-semibold text-slate-900 dark:text-white mb-2">Description</h6>
      <p class="text-slate-600 dark:text-slate-300 text-sm"><%= simple_format(@service_hour.description) %></p>
    </div>

    <% if @service_hour.photos.attached? %>
      <h5 class="font-semibold text-slate-900 dark:text-white mb-3">Photo Proof</h5>
      <div class="grid grid-cols-2 md:grid-cols-3 gap-3 mb-4">
        <% @service_hour.photos.each do |photo| %>
          <div>
            <%= image_tag photo, class: "w-full rounded-xl" %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>

  <div>
    <% if @service_hour.pending? %>
      <div class="bg-white dark:bg-slate-800 rounded-2xl border border-gray-200 dark:border-slate-700">
        <div class="px-6 py-4 border-b border-gray-200 dark:border-slate-700">
          <h5 class="font-semibold text-slate-900 dark:text-white">Review</h5>
        </div>
        <div class="p-6">
          <%= form_with(url: review_admin_service_hour_path(@service_hour), method: :patch, local: true) do |f| %>
            <div class="mb-4">
              <%= f.label :admin_comment, "Comment", class: "block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1" %>
              <%= f.text_area :admin_comment, class: "w-full px-3 py-2 border border-slate-200 dark:border-slate-600 rounded-xl bg-slate-50 dark:bg-slate-700 text-sm focus:outline-none focus:ring-2 focus:ring-primary-500", rows: 3, name: "admin_comment" %>
            </div>
            <div class="space-y-2">
              <button type="submit" name="status" value="approved" class="w-full bg-status-approved text-white rounded-xl px-4 py-2.5 text-sm font-medium hover:opacity-90">
                &#10003; Approve
              </button>
              <button type="submit" name="status" value="rejected" class="w-full bg-status-rejected text-white rounded-xl px-4 py-2.5 text-sm font-medium hover:opacity-90">
                &#10005; Reject
              </button>
            </div>
          <% end %>
        </div>
      </div>
    <% else %>
      <div class="bg-white dark:bg-slate-800 rounded-2xl border border-gray-200 dark:border-slate-700 p-6">
        <p class="text-sm mb-2"><span class="font-medium text-slate-500 dark:text-slate-400">Reviewed by:</span> <span class="text-slate-900 dark:text-white"><%= @service_hour.reviewer&.full_name || "N/A" %></span></p>
        <p class="text-sm mb-2"><span class="font-medium text-slate-500 dark:text-slate-400">Reviewed at:</span> <span class="text-slate-900 dark:text-white"><%= @service_hour.reviewed_at&.strftime("%B %d, %Y %I:%M %p") %></span></p>
        <% if @service_hour.admin_comment.present? %>
          <p class="text-sm"><span class="font-medium text-slate-500 dark:text-slate-400">Comment:</span> <span class="text-slate-600 dark:text-slate-300"><%= @service_hour.admin_comment %></span></p>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

<% if @service_hour.editor.present? %>
  <div class="bg-blue-50 dark:bg-blue-950 border border-blue-200 dark:border-blue-800 rounded-xl p-4 mt-6 text-sm text-blue-800 dark:text-blue-300">
    &#9998; Last edited by <strong><%= @service_hour.editor.full_name %></strong>
    on <%= @service_hour.edited_at&.strftime("%B %d, %Y %I:%M %p") %>
  </div>
<% end %>

<div class="mt-4 flex items-center space-x-3">
  <%= link_to "Edit", edit_admin_service_hour_path(@service_hour), class: "border border-primary-500 text-primary-500 px-5 py-2 rounded-xl text-sm font-medium hover:bg-primary-50 dark:hover:bg-primary-950" %>
  <%= link_to "← Back", admin_service_hours_path, class: "border border-slate-300 dark:border-slate-600 text-slate-700 dark:text-slate-300 px-5 py-2 rounded-xl text-sm font-medium hover:bg-slate-50 dark:hover:bg-slate-700" %>
</div>
