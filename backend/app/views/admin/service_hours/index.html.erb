<h1 class="text-2xl font-semibold text-slate-900 mb-6">Service Hours Review</h1>

<div class="flex items-center space-x-2 mb-6">
  <%= link_to "All", admin_service_hours_path, class: "px-4 py-1.5 rounded-full text-sm font-medium #{params[:status].blank? ? 'bg-primary-500 text-white' : 'border border-slate-300 text-slate-600 hover:bg-slate-50'}" %>
  <%= link_to "Pending", admin_service_hours_path(status: :pending), class: "px-4 py-1.5 rounded-full text-sm font-medium #{params[:status] == 'pending' ? 'bg-status-pending text-white' : 'border border-slate-300 text-slate-600 hover:bg-slate-50'}" %>
  <%= link_to "Approved", admin_service_hours_path(status: :approved), class: "px-4 py-1.5 rounded-full text-sm font-medium #{params[:status] == 'approved' ? 'bg-status-approved text-white' : 'border border-slate-300 text-slate-600 hover:bg-slate-50'}" %>
  <%= link_to "Rejected", admin_service_hours_path(status: :rejected), class: "px-4 py-1.5 rounded-full text-sm font-medium #{params[:status] == 'rejected' ? 'bg-status-rejected text-white' : 'border border-slate-300 text-slate-600 hover:bg-slate-50'}" %>
</div>

<div class="bg-white rounded-2xl border border-gray-200 overflow-hidden">
  <table class="w-full text-sm">
    <thead class="bg-slate-50">
      <tr>
        <th class="text-left px-4 py-3 text-slate-500 font-medium">Volunteer</th>
        <th class="text-left px-4 py-3 text-slate-500 font-medium">Date</th>
        <th class="text-left px-4 py-3 text-slate-500 font-medium">Hours</th>
        <th class="text-left px-4 py-3 text-slate-500 font-medium">Category</th>
        <th class="text-left px-4 py-3 text-slate-500 font-medium">Group</th>
        <th class="text-left px-4 py-3 text-slate-500 font-medium">Status</th>
        <th class="px-4 py-3"></th>
      </tr>
    </thead>
    <tbody class="divide-y divide-gray-100">
      <% @service_hours.each do |sh| %>
        <tr class="hover:bg-slate-50">
          <td class="px-4 py-3 text-slate-900"><%= sh.user&.full_name || "Deleted User" %></td>
          <td class="px-4 py-3 text-slate-600"><%= sh.service_date.strftime("%m/%d/%Y") %></td>
          <td class="px-4 py-3 text-slate-600"><%= sh.hours %></td>
          <td class="px-4 py-3 text-slate-600"><%= sh.category.name %></td>
          <td class="px-4 py-3 text-slate-600"><%= sh.group&.name || "—" %></td>
          <td class="px-4 py-3">
            <span class="px-2 py-0.5 rounded-full text-xs font-medium <%= sh.approved? ? 'bg-green-50 text-status-approved' : sh.pending? ? 'bg-amber-50 text-status-pending' : 'bg-red-50 text-status-rejected' %>">
              <%= sh.status.humanize %>
            </span>
          </td>
          <td class="px-4 py-3"><%= link_to "Review", admin_service_hour_path(sh), class: "text-primary-500 hover:text-primary-700 text-xs font-medium" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<div class="mt-4">
  <%== bootstrap_series_nav(@pagy) if @pagy.pages > 1 %>
</div>
