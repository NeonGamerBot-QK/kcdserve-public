import { View, Text } from 'react-native';

// Stub — the FAB redirects to /log-hours/ stack, this screen is never shown
export default function LogStub() {
  return (
    <View className="flex-1 items-center justify-center bg-slate-50">
      <Text className="font-inter text-slate-500">Redirecting...</Text>
    </View>
  );
}
